Teach kai something new.

If `$ARGUMENTS` contains structured fields (category, topic, content, tags), call `mcp__kai__teach` in direct mode with those fields.

Otherwise, call `mcp__kai__teach` with:
- `input`: `$ARGUMENTS`

Show what was saved.
