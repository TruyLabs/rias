Install rias slash commands for Claude Code.

Call the `mcp__rias__setup_commands` tool to get all command files.

For each command in the result, write the file to `~/.claude/commands/rias/<name>.md`.

After writing all files, tell the user to restart Claude Code to pick up the new commands.
