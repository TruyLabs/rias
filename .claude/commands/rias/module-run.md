Run a rias plugin/module to fetch external data into the brain.

Call the `mcp__rias__module_run` tool with:
- `name`: `$ARGUMENTS` (if provided; omit to run all enabled modules)

Show the import results.
